# 开发日志2 — 栅格地图与图层控制

---

## 进展小结

本周重点开发了独立的栅格地图页面（`raster-map.html` + `raster-map.js`）。该页面不依赖天地图在线底图，而是直接通过 GeoServer WMS 服务加载三期土地利用栅格数据。在 `vite.config.js` 中配置了 `/geoserver` 代理转发，解决开发环境跨域问题。

成功加载了 1980、2000、2020 三年的栅格图层，每层均使用 `ol/source/ImageWMS` 对接 GeoServer 发布的服务，工作区为 `gcsj`，分辨率为 30m。左侧控制面板实现了图层可见性切换（点击开关按钮）和透明度调节（range 滑块 0-100%），以及视图一键重置功能。

---

## 近期计划

下周回到主系统页面开发，重点实现地图点击选中区县的功能，并开始通过 Axios 对接后端 Flask 接口，实现区县数据查询与图表联动展示。

---

## 问题与感悟

1. **多层栅格渲染性能**：三个 WMS 图层同时可见时，缩放操作略有卡顿。通过默认将透明度设为 0.8，并建议用户按需关闭不需要的图层来缓解。后续可考虑使用 TileWMS 替代 ImageWMS 来优化。