# Week 16 Backend Work Notes

本周完成内容：

- 配合中期检查，确认后端本地数据文件已放入项目目录。
- 检查三期土地利用栅格数据、县级行政边界数据和 SQLite 数据库文件是否齐全。
- 增加 `scripts/week16_backend_check.py`，用于检查中期演示前的数据与数据库状态。
- 检查 `landuse_data` 表结构、年份列表、区县数量和样例记录。
- 更新 `.gitignore`，避免将遥感数据、数据库、日志和缓存文件提交到远程仓库。

本周工作重点是完成中期检查前的后端数据就绪检查，为接口运行和前后端联调做准备。
