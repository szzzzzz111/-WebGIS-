# Week 18 Backend Work Notes

本周完成内容：

- 补充 `GET /api/docs` 接口，返回后端核心API清单，便于前端联调和文档整理。
- 统一上传接口、可用年份接口的错误返回格式。
- 增加 GeoTIFF 文件名校验，支持 `.tif` 和 `.tiff` 后缀。
- 补充多区县面积汇总、转移矩阵相加、上传文件名校验和接口文档生成的基础测试。
- 继续保持数据文件、数据库、日志和缓存文件不提交到远程仓库。

本周工作重点是完善异常处理、接口文档和测试整理，为最终阶段联调与验收做准备。
