# 开发日志4 — 变化分析与转移矩阵【中期检查】

## 进展小结

本周重点完成了变化指数与转移矩阵模块，为中期检查做准备。主要工作包括：

**变化指数**：接入后端 API `/api/change-indices?county_id={id}&start_year={year}&end_year={year}`，获取指定区县在分析时间段内的各项变化指数数据。控制面板新增"分析指标"下拉选择（全部指标/动态度/变化率/综合指数/多样性指数），切换后指数网格按类型筛选展示。每种指数以卡片形式排列，绿色边框表示正值增长、红色边框表示负值减少、黄色表示中性。

**时间段设置**：在时间维度面板新增起始/结束年份下拉选择框和"更新分析时间段"按钮，切换后变化指数和转移矩阵按新时间段重新加载。

**转移矩阵**：通过 API `/api/transition-matrix?county_id={id}&start_year={year}&end_year={year}` 获取转移矩阵数据，以 HTML 表格形式渲染。行表示起始年份地类、列表示终止年份地类，对角单元格（未变化）蓝色突出，变化单元格红色突出，无转换区域灰色显示。带颜色图例说明。矩阵区域可通过紫色按钮显示/隐藏切换。

**中期材料**：协助撰写需求分析文档中"前端可视化"章节，准备中期检查 PPT 中系统演示部分。

---

## 近期计划

下周实现栅格数据上传功能，允许用户上传新年代的 .tif 文件扩展系统数据，同时完善全局 Loading 和通知组件。

